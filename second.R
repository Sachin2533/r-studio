install.packages("spatial")
library(spatial)

library(boot)

packageDescription("boot")

library(help=boot)

install.packages("boot")

install.packages("cluster")

installed.packages()

#. remove.packages(" package_name ")

update.packages("cluster")

detach("package:cluster", unload = TRUE)

ls() 

getwd()

# setwd("/Users).  ----- to change the directory

x=1:200
x

x+x

x+x+x

ls()

# rm() -----to remove the objects/variables

q()


# Assignment Operator
x <-10
y = 20
x
y
z=x+y
z
q=x*2
q

a= "apple"
a1 <-'APPLE'
a
a1

is.numeric(x)
is.numeric(y)
y="Sachin"
is.character(y)
is.numeric(y)

y= as.character(x)
is.numeric(y)

is.character(y)
y

mode(x)
mode(a)

# modes : logical,integer,double, complex, raw, list, character, name, expression,name,symbol,function